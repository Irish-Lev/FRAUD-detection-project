# ── Raw data (download from Kaggle — never commit) ───────────────────────────
data/raw/

# ── Large processed CSVs (regenerated by running notebooks 02 and 04) ────────
# Plots, model_comparison.csv and phase4a_metrics.json are NOT excluded (small + needed)
data/processed/X_train.csv
data/processed/X_test.csv
data/processed/y_train.csv
data/processed/y_test.csv
data/processed/cnn_predictions.csv

# ── FinBERT embeddings (large .npy files — regenerated by running notebook 03) ─
data/embeddings/


# ── SQLite databases (regenerated by running notebooks) ──────────────────────
data/fraud_predictions.db
mlflow.db

# ── Models ───────────────────────────────────────────────────────────────────
# xgb_fraud_model.pkl intentionally kept (1.3 MB — loaded by Streamlit Cloud)
models/cnn_fraud_model.keras
models/cnn_scaler.pkl
models/*.joblib

# ── MLflow experiment logs ────────────────────────────────────────────────────
mlruns/
notebooks/mlruns/

# ── Virtual environment ───────────────────────────────────────────────────────
venv/
.venv/
env/

# ── Environment variables — NEVER commit real API keys ───────────────────────
.env
*.env

# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── OS / editor files ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/settings.json

# ── Streamlit secrets ─────────────────────────────────────────────────────────
.streamlit/secrets.toml

# ── Project-internal files (not for public repo) ──────────────────────────────
CLAUDE.md
prompts/
pipeline_trace.py
*.pdf
frontend_slides_main/
presentation/presenter_script.md
presentation/fraud_detection_presentation.html
